ang-main-header
ang-main-nav

.row.main-content#mainContent
    .col-12.col-md-7.page-hdr
        h1 Manage Users
    button.btn.btn-secondary.col-2.mr-1((click)="exportCSV()") Export
    button.btn.btn-primary.col-2 {{ 'USERS.BUTTONS.REGISTER_USER' | translate }}
    .grid-cntnr.col-12
        ng-table([config]="config", (tableChanged)="onChangeTable(config)", (cellClicked)="onCellClick($event)", [rows]="rows", [columns]="columns")
        //- pagination(*ngIf="config.paging", class="pagination-sm", [(ngModel)]="page", [totalItems]="length", [itemsPerPage]="itemsPerPage", [maxSize]="maxSize", [boundaryLinks]="true", [rotate]="false", (pageChanged)="onChangeTable(config, $event)", (numPages)="numPages = $event")
        //- pre(*ngIf="config.paging",class="card card-block card-header") | Page: {{page}} / {{numPages}}
        
    .error-msg(*ngIf="messages.serviceError") {{ 'USERS.MESSAGES.SERVICE_ERROR' | translate }}
    .error-msg(*ngIf="messages.unauthorized") {{ 'USERS.MESSAGES.UNAUTHORIZED' | translate }}


    